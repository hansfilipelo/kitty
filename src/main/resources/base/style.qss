QLabel {
    min-width: 40em;
    max-width: 100em;
    min-height: 21em;
    font-style: bold;
    text-align: center;
    background: transparent;
}

QWidget {
  background: transparent;
}

QPushButton {
    max-width: 10em;
}
